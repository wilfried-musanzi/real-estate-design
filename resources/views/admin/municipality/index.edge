@layout('admin/base')
@set('title', "Administration")
@section('body')
@include('partials/flash')
    <table class="table table-striped">
        <thead>
            <tr>
                <th>#</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @each(municipality in municipalities)
              <tr>
                <td>{{ municipality.id }}</td>
                <td>{{ municipality.name }}</td>
                <td>
                    <a href="{{ route("municipality.edit", { id: municipality.id }) }}" class="btn btn-primary">Editer</a>
                    <form action="{{ route("municipality.delete", { id: municipality.id }) }}" method="post" style="display:inline-block">
                        <button type="submit" class="btn btn-danger">Supprimer</button>
                    </form>
                </td>
            </tr>
            @end
        </tbody>
    </table>
    <div class="d-flex justify-content-end mb-4">
        <a href="{{ route("municipality.new")}}" class="btn btn-primary">Add New</a>
    </div>
@end
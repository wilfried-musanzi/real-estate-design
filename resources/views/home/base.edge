<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@200;400;500;600&display=swap" rel="stylesheet">
    <title>{{ title }}</title>
    @entryPointStyles('app')
    @entryPointScripts('app')
</head>
<body>
     <div class="nk-header nk-header-fixed is-light">
        <div class="container">
            <div class="nk-header-wrap">
                <div class="nk-news-text">
                    <a class="nav-link {{ controller == 'homeController' ? 'active' : ' '}}" href="{{ route("home") }}">Home</a>
                    @if(await bouncer.allows('accedToAdminPanel'))
                        <a class="nav-link " href="{{ route("admin") }}">Dashboard</a>
                    @else
                        <a class="nav-link {{ controller == 'propertiesController' ? 'active' : ' '}}" href="{{ route("properties.index") }}">Properties</a>
                    @end
                </div>
                <div class="nk-header-tools">
                    <ul class="nk-quick-nav">
                        <li class="dropdown user-dropdown">
                            <div class="user-toggle">
                                @if(auth.isLoggedIn)
                                    <div class="user-avatar sm">
                                        <em class="icon ni ni-user-alt"></em>
                                    </div>
                                    <div class="user-info d-none d-md-block">
                                        <div class="user-status">{{ auth.user.role == 'admin' ? 'Administrator' : 'Standard' }}</div>
                                        <div class="user-name">{{ auth.user.email }}</div>
                                        <li>
                                            <a href="{{ route('logout')}}"><em class="icon ni ni-signout"></em><span>Logout</span></a>     
                                        </li>
                                    </div>
                                @else
                                    <a class="nav-link {{ controller == 'loginController' ? 'active' : ' '}}" href="{{ route("login") }}">Connexion</a>
                                    <a class="nav-link {{ controller == 'signupController' ? 'active' : ' '}}" href="{{ route("signup") }}">Inscription</a> 
                                @end
                            </div>      
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <section class="container" style="margin-top: 5rem">
        @!section('body')
    </section>
</body>
</html>